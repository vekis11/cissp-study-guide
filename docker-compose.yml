services:
  cissp-study:
    build: .
    ports:
      - "8080:8080"
    environment:
      DATABASE_PATH: /data/cissp_study.db
      SERVE_STATIC: "true"
      CORS_ALLOW_ALL: "true"
    volumes:
      - cissp-data:/data
    restart: unless-stopped

volumes:
  cissp-data:
